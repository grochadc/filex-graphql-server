{
  "name": "filex-graphql-server",
  "version": "1.0.0",
  "description": "",
  "engines": {
    "node": "14.x"
  },
  "main": "server.js",
  "scripts": {
    "start": "node dist/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx typescript@latest",
    "dev": "nodemon --ext ts,tsx --exec 'tsc && node' dist/server.js",
    "publish": "git push heroku master && heroku logs --tail"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/gapi": "^0.0.39",
    "@types/node": "^14.11.2",
    "apollo-datasource-rest": "^0.9.4",
    "apollo-server": "^2.18.1",
    "dataloader": "^2.0.0",
    "dotenv": "^8.2.0",
    "firebase": "^7.21.1",
    "googleapis": "39",
    "graphql": "^15.3.0",
    "node-fetch": "^2.6.1",
    "ramda": "^0.27.1",
    "typescript": "^4.0.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
